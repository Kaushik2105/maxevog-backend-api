/**
 * Notification Constants
 */
const NOTIFICATION_TYPES = Object.freeze({
  APPLICATION_DEADLINE: 'APPLICATION_DEADLINE',
  ADMIT_CARD_AVAILABLE: 'ADMIT_CARD_AVAILABLE',
  EXAM_REMINDER: 'EXAM_REMINDER',
  RESULT_AVAILABLE: 'RESULT_AVAILABLE',
  APPLICATION_UPDATE: 'APPLICATION_UPDATE',
  MEMBERSHIP: 'MEMBERSHIP',
  SYSTEM: 'SYSTEM',
});

const ALL_NOTIFICATION_TYPES = Object.freeze(Object.values(NOTIFICATION_TYPES));

const NOTIFICATION_CHANNELS = Object.freeze({
  EMAIL: 'EMAIL',
  TELEGRAM: 'TELEGRAM',
});

const ALL_NOTIFICATION_CHANNELS = Object.freeze(Object.values(NOTIFICATION_CHANNELS));

const NOTIFICATION_STATUSES = Object.freeze({
  PENDING: 'PENDING',
  SENT: 'SENT',
  FAILED: 'FAILED',
  READ: 'READ',
});

const ALL_NOTIFICATION_STATUSES = Object.freeze(Object.values(NOTIFICATION_STATUSES));

module.exports = {
  NOTIFICATION_TYPES,
  ALL_NOTIFICATION_TYPES,
  NOTIFICATION_CHANNELS,
  ALL_NOTIFICATION_CHANNELS,
  NOTIFICATION_STATUSES,
  ALL_NOTIFICATION_STATUSES,
};
